# Floor versions for QualReAI fork.
# We rely on pip's resolver at Docker build time rather than the upstream's
# strict pin list (which targeted Python 3.8 + transformers 4.23 from 2022).
# Whisper-large-v3-turbo + return_timestamps=True needs transformers >= 4.45.
fastapi>=0.103,<0.116
pydantic>=1.10.13,<2
python-multipart>=0.0.7
starlette>=0.27,<0.40
uvicorn>=0.23
click>=8.1
email-validator>=2.0
statsd>=4.0
sentry-sdk>=1.40
torch>=2.2
torchaudio>=2.2
transformers>=4.45,<5
huggingface-hub>=0.25
tokenizers>=0.20
librosa>=0.10
numpy>=1.26,<2
